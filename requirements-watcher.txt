# Slim deps for the GitHub Actions alert watcher (no Streamlit — faster installs,
# fewer Action minutes). numpy comes in transitively via pandas.
yfinance>=1.5
nsepython>=2.9
requests>=2.32
pandas>=2.2
pyyaml>=6.0
cryptography>=42.0

